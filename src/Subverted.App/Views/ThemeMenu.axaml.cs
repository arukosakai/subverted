using Avalonia.Controls;

namespace Subverted.App.Views;

/// <summary>The rail's theme picker: oto's presets, each with a swatch of its own colours.</summary>
public sealed partial class ThemeMenu : UserControl
{
    public ThemeMenu()
    {
        InitializeComponent();
    }
}
